---
state:
  request_id: R-20260708-0639
  title: Reconstruct context.md from legacy archive
  status: analysis_ready
  input_verification_result: valid
  context_verification_result: valid
options:
  minimum_questions: 5
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input

### Goal

Reconstruct the workspace's `.aib_memory/context.md` (and optionally other memory files) from the archived legacy `.aib_memory/` content located at `.aib_memory/archives/legacy_20260708-063224/`. The current `context.md` is a valid placeholder that must be replaced with the full workspace context through semantic reconstruction. Run this as an `aib-modify.md` request.

### Sources

- Primary source: `.aib_memory/archives/legacy_20260708-063224/context.md` — the full legacy context.md archived from the pre-upgrade memory.
- Optional source: `.aib_memory/archives/legacy_20260708-063224/input.md` — archived legacy input.md; may provide useful background; skip if absent or structurally incompatible.
- Optional source: `.aib_memory/archives/legacy_20260708-063224/aib-setup.yaml` — archived legacy setup; reference only for `default_questions_number` value.
- Conventions to follow: `.aib_brain/conventions/context-convention.md`, `.aib_brain/conventions/input-convention.md`, `.aib_brain/conventions/q-block-convention.md`.

### Reconstruction Targets

- context.md — reconstruct all six sections (Product, Concepts, Requirements, Solution, File Structure, and optionally References) from the archived legacy context.md using semantic interpretation. Conform to context-convention.md.
- aib-setup.yaml — already seeded correctly by the upgrade script. Verify that `memory_version` matches the current brain version and `default_questions_number` matches the archived value. Update only if discrepant.

### Constraints

- Do NOT restore requests from the archive. Legacy requests are at `.aib_memory/archives/legacy_20260708-063224/requests/` for reference only.
- Do NOT overwrite `instructions.md` — it was copied unchanged from the archive.
- If optional source files are absent or structurally incompatible, skip them without failure.
- Legacy requests were archived and are not restored. Find them at `.aib_memory/archives/legacy_20260708-063224/requests/` if needed.
