## Active request
No active request

## Options
- Minimum questions: 0

## Input

Issues with dim_settlement dimension

"Попово" and "Търговище" have multiple keys because there is city and vilage with same name. In `data/schema/dim_settlement.csv` are added 'гр. ' for city and 'с. ' for vilage infont of the names. The entries in the database should be fixed.

There are also cases like "Нови Искър" where the ЕКАТТЕ is written with and without leadin zeroes and this has caused multiple keys for same EKATTE. Find a way to normalize and avoid duplication like this.