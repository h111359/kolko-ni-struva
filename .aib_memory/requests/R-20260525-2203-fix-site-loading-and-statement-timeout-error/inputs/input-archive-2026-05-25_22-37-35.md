## Status
R-20260525-2203 — Fix site loading and statement timeout error
State: questions_generated

## Options
- Minimum questions: 0

## Input

## Questions

**Q001**: How should the total result count be provided for pagination after removing `COUNT(*) OVER()` from `get_landing_page_rows`?
> **Why this matters:** The current `LandingPage` uses `totalCount` to calculate total pages and render the pagination bar; the strategy chosen determines whether a new RPC is added or the pagination model changes.
- [x] Option A: Add a separate `get_landing_page_count` RPC with identical filter parameters; call it in parallel with the rows RPC; render pagination with the count once it resolves. *(recommended)*
- [ ] Option B: Remove total count entirely; show only the current page number; replace "last page" navigation with a "Next" button that is hidden when fewer than PAGE_SIZE rows are returned.
- [ ] Option C: Return an estimated count from PostgreSQL table statistics (via `pg_class.reltuples`); accept that the estimate may be imprecise.
- [ ] Other: ___

**Q002**: Should `loadOptions` in `LandingPage.jsx` be hardened against partial RPC failures as part of this fix?
> **Why this matters:** Currently one failing option RPC empties all five dropdowns; whether to address this determines the scope of changes to `LandingPage.jsx`.
- [x] Option A: Keep `Promise.all`; rely on the root-cause fix (removing `COUNT(*) OVER()`) to prevent future option RPC failures under normal conditions. *(recommended)*
- [ ] Option B: Replace `Promise.all` with `Promise.allSettled` in `loadOptions` so that one failing dimension RPC does not clear the other four dropdowns.
- [ ] Other: ___

