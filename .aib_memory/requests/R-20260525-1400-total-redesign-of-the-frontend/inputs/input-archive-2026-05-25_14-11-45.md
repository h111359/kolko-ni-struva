## Status
No active request
State: idle

## Options
- Minimum questions: 0

## Input

Make total redesigh of the frontend.
Remove pages "Начало", "Цени по категория", "Продукти", "Сравнение по места".
Move the intro text from "Начало" to page "Файлове"
Make page "Файлове" to be the landing page and remove the button "Файове" with entry - the detail table, not the list of files but with added field for the name of the file when the most detaied granularity is shown. Show only "Цена без промоция" which should be the current "Цена" and "Цена" which should be the minimum between "Цена" and "Промо цена" if "Промо цена" is available - otherwise equal to "Цена"

The landing page should show directly the data from the last loaded day (first page from the pagination)
The landing page should introduce dropdowns for filtering on:
- "Населено място"
- "Категория"
- "Верига"
- "Магазин"
- "Дата"


In addition to be added filter based on text input (case insensitive), filter all products with substring the string entered with name 

In addition to the filter to be added number filtering:
- "Цена от" - with check for proper decimal number
- "Цена до" - with check for proper decimal number

Add also possibility to group by on 2 levels - combination from:
- "Населено място"
- "Категория"
- "Верига"
- "Магазин"
- "Дата"
On first grouping position could stand any of those, on the second - some of the rest.

When grouping, show all the metrics "Цена" and "Промо цена" with aggregations - average, min, max