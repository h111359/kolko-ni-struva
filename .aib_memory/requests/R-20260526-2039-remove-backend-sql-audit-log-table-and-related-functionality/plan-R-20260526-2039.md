## Goal

Remove the Supabase `backend_sql_audit_log` table and all repository-owned functionality that provisions it, writes to it, or documents it as a supported product feature. Keep the remaining Supabase sync flow and landing-page analytics behavior unchanged.

## Constraints

- Do not modify files under `.aib_brain/`.
- Keep the implementation limited to the backend SQL audit feature surface in `src/load_supabase.py`, its direct tests, and affected documentation.
- Do not read or use artifacts from Closed request folders.
- Do not add new third-party dependencies.

## Success criteria

- `src/load_supabase.py` no longer creates, indexes, prunes, or writes to `backend_sql_audit_log`.
- The Supabase sync helpers for dimension upserts, lookback inserts, and landing-page projection refresh still execute through the existing code path without audit-specific wrappers.
- Targeted unit tests covering `src/load_supabase.py` pass after expectations are updated for the removed audit feature.
- Current workspace documentation no longer describes `backend_sql_audit_log` as part of the live product.

## Plan

### Task 1: Remove backend SQL audit implementation

#### Intent

Delete the backend SQL audit table feature from the Supabase sync module while preserving the rest of the sync behavior.

#### Outputs

Updated `src/load_supabase.py`.

#### Procedure

Inspect and edit `src/load_supabase.py` to remove `backend_sql_audit_log` constants, DDL, index DDL, audit insert SQL, audit helper functions, audit-specific batching helpers, and audit-log pruning logic.

Edit `src/load_supabase.py` to route remaining SQL execution through non-audited helpers and to keep `create_tables`, dimension upserts, lookback inserts, projection refresh, and retention pruning functional without the removed audit abstraction.

Edit `src/load_supabase.py` to drop the audit-table cleanup from the main sync flow and remove any user-facing status messages that mention backend SQL audit retention.

#### Done criteria

`src/load_supabase.py` contains no live references to `backend_sql_audit_log`, audit insert statements, or audit-log pruning.

#### Dependencies

None.

#### Risk notes

The existing audit helpers also centralize SQL execution for some write paths, so the replacement must preserve batching and commit behavior for dimension upserts and projection refresh.

### Task 2: Update request-scoped validation coverage

#### Intent

Align unit tests with the removed audit-table feature without weakening coverage of the remaining Supabase sync behavior.

#### Outputs

Updated `tests/test_load_supabase.py`.

#### Procedure

Edit `tests/test_load_supabase.py` to remove audit-table assertions and audit-helper test cases that no longer apply after the feature removal.

Edit `tests/test_load_supabase.py` to assert the retained `src/load_supabase.py` behavior that still matters for this request, including DDL execution counts, non-audited SQL execution, batch usage, and projection refresh flow.

#### Done criteria

`tests/test_load_supabase.py` fails if the removed audit feature is reintroduced into DDL or main sync behavior and still validates the unaffected sync helpers.

#### Dependencies

Task 1.

#### Risk notes

The mocked cursor fixtures currently encode audit-specific sibling-cursor behavior, so they may need simplification to keep the tests readable and deterministic.

### Task 3: Run focused validation

#### Intent

Verify that the removed audit feature does not break the loader module’s supported behavior.

#### Outputs

Validation results from `python -m unittest tests.test_load_supabase`.

#### Procedure

Run `python -m unittest tests.test_load_supabase` from the workspace root and capture the result for this request.

If `python -m unittest tests.test_load_supabase` fails, edit `src/load_supabase.py` or `tests/test_load_supabase.py` to fix the local defect exposed by the failure and rerun the same command until the request scope is green or a concrete blocker is identified.

#### Done criteria

`python -m unittest tests.test_load_supabase` passes with no unresolved failures related to this request.

#### Dependencies

Task 1, Task 2.

#### Risk notes

The test module uses mocked `psycopg2` imports, so failures are likely to reflect interface changes in the loader helpers rather than environment issues.

### Task 4: Refresh documentation and request artifacts

#### Intent

Update live documentation to remove the audit-table feature and record the implementation outcome for the request.

#### Outputs

Updated `README.md`, updated `.aib_memory/context.md`, and request-scoped `implementation.md` in the active request folder.

#### Procedure

Edit `README.md` to remove or rewrite statements that describe `backend_sql_audit_log` as a current Supabase feature, retention policy, or operator-facing audit surface.

Execute `.aib_brain/prompts/aib-refresh-context.md` to fully regenerate `.aib_memory/context.md` after the implementation is validated.

Create or append the request-scoped implementation entry in `.aib_memory/requests/R-20260526-2039-remove-backend-sql-audit-log-table-and-related-functionality/implementation.md` using `.aib_brain/conventions/implementation-convention.md` after code changes and validation are complete.

#### Done criteria

Live documentation reflects the removal of the backend SQL audit table and the request folder contains an implementation record for the completed change.

#### Dependencies

Task 3.

#### Risk notes

The refreshed context must be generated after validation so it documents the final code state rather than an intermediate edit.