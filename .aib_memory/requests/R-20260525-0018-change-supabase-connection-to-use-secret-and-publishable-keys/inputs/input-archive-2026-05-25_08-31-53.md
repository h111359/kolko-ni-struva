## Status
R-20260525-0018 — Change Supabase connection to use secret and publishable keys
State: questions_generated

## Options
- Minimum questions: 0

## Input

## Questions

**Q001**: How should the backend ETL connect to Supabase after this migration?
> **Why this matters:** The backend (`src/load_supabase.py`) currently uses `DATABASE_URL` + psycopg2 (direct PostgreSQL) and does not use any Supabase REST API key. The `sb_secret_...` key is a REST API key, not a PostgreSQL password, so it cannot be substituted directly. The decision determines whether the backend architecture changes and the scope of the implementation.
- [x] Option A: Keep psycopg2/DATABASE_URL for all backend operations; the `sb_secret_...` key does not apply to the backend in this migration — only the frontend changes. *(recommended)*
- [ ] Option B: Add `supabase-py` library alongside psycopg2 using `sb_secret_...`; keep psycopg2 for DDL and bulk ops, use supabase-py for any new REST-based operations.
- [ ] Option C: Replace psycopg2 entirely with `supabase-py` using `sb_secret_...` (major rewrite; note: supabase-py cannot execute DDL — schema provisioning would require a separate approach).
- [ ] Other: ___

**Q002**: Should the frontend environment variable be renamed to match the new key terminology?
> **Why this matters:** `VITE_SUPABASE_ANON_KEY` is hardcoded in `react-app/src/lib/supabase.js`, `menu.py` (3 places), `tests/test_menu.py`, and `.env.example`. A rename requires updating all these files plus any existing `.env` files and the Netlify dashboard environment variables.
- [x] Option A: Rename to `VITE_SUPABASE_PUBLISHABLE_KEY` — aligns with Supabase's new terminology; requires updating all references in code, tests, `.env.example`, and Netlify dashboard. *(recommended)*
- [ ] Option B: Keep `VITE_SUPABASE_ANON_KEY` — change only the key value in `.env`; no code changes required; variable name becomes a legacy label.
- [ ] Option C: Rename to `VITE_SUPABASE_KEY` — generic name; not tied to any key format.
- [ ] Other: ___

**Q003**: How should `tests/test_menu.py::TestViteAnonKeyRole` be updated for the non-JWT new key format?
> **Why this matters:** The test currently decodes the key as a JWT and asserts `role='anon'`. The new `sb_publishable_...` keys are NOT JWTs — they will cause the test to hard-fail rather than skip. The security guarantee (ensuring a secret key isn't in the bundle) must still be provided.
- [x] Option A: Replace JWT decode with a prefix check — assert key starts with `sb_publishable_` and fail if it starts with `sb_secret_`. *(recommended)*
- [ ] Option B: Branch on key format — if three dot-separated segments, decode as JWT; otherwise assert `sb_publishable_` prefix (handles both legacy and new keys).
- [ ] Option C: Delete `TestViteAnonKeyRole` entirely — remove the security test.
- [ ] Other: ___

**Q004**: How should the Netlify build environment variable be updated for the new key?
> **Why this matters:** Vite injects `VITE_` variables at build time from the Netlify site's environment settings (dashboard or CLI). Renaming `VITE_SUPABASE_ANON_KEY` without updating Netlify will silently produce a broken production bundle. There is no automated mechanism in the codebase to update Netlify env vars.
- [x] Option A: Document the required manual Netlify dashboard update in `.env.example` and `README.md` — add a migration note explaining which env var must be created/renamed in the Netlify site settings. *(recommended)*
- [ ] Option B: Update `deploy_netlify.py` to validate that the new VITE_ key variable is present before deploying — add a pre-deploy guard that prints a clear error if the variable is missing.
- [ ] Option C: Add the env var declaration to `react-app/netlify.toml` so the required variables are self-documenting in the repository.
- [ ] Other: ___

**Q005**: Should the migration be a hard cutover or a soft backward-compatible transition?
> **Why this matters:** A hard cutover removes the old variable name immediately, requiring all environments (developer machine `.env`, Netlify dashboard) to be updated at once. A soft approach adds fallback logic to support both old and new variable names during a transition period.
- [x] Option A: Hard cutover — remove old variable name/references immediately; all environments must be updated at once. *(recommended)*
- [ ] Option B: Soft migration — read new variable name with fallback to old name in `supabase.js` and `menu.py`; remove fallback in a follow-up request.
- [ ] Option C: Not applicable — keep existing variable name (VITE_SUPABASE_ANON_KEY) and only change the value (this option only applies if Q002 resolves to Option B).
- [ ] Other: ___

**Q006**: How should `menu.py`'s credential validation guard be updated?
> **Why this matters:** `action_local_preview()` in `menu.py` checks for `VITE_SUPABASE_ANON_KEY` before running the React build. If the variable is renamed, the guard must be updated — otherwise it will report a missing credential even when the new variable is set, preventing local preview from working.
- [x] Option A: Update `menu.py` to check the new variable name only (consistent with Q002 Option A). *(recommended)*
- [ ] Option B: Check both old and new variable names — guard accepts either; remove old name check in a follow-up.
- [ ] Option C: No change needed (only applies if Q002 resolves to keep `VITE_SUPABASE_ANON_KEY`).
- [ ] Other: ___

**Q007**: Should the `supabase` Python library be added to `requirements.txt`?
> **Why this matters:** If Q001 resolves to Option B or C (adding or switching to supabase-py), `supabase` must be added as a Python dependency. If Q001 resolves to Option A (keep psycopg2), no change is needed.
- [ ] Option A: Add `supabase>=2.0.0` to `requirements.txt` (applicable if Q001 = Option B or C). *(recommended if Q001 ≠ Option A)*
- [x] Option B: No change to `requirements.txt` (applicable if Q001 = Option A and backend keeps psycopg2 only).
- [ ] Other: ___

**Q008**: Should `createClient()` in `react-app/src/lib/supabase.js` be updated with explicit auth options?
> **Why this matters:** The product uses Supabase only for data access (no user authentication). Default `createClient` options include session persistence and token refresh, which are unnecessary overhead for a read-only public app. Adding explicit options (`auth: { persistSession: false, autoRefreshToken: false }`) makes the intent explicit.
- [x] Option A: Keep `createClient(url, key)` with no extra options — existing behaviour is correct; no code change needed. *(recommended)*
- [ ] Option B: Add `auth: { persistSession: false, autoRefreshToken: false, detectSessionInUrl: false }` to make pure-data-access intent explicit.
- [ ] Other: ___

**Q009**: Should one or multiple `sb_secret_...` keys be created for backend use?
> **Why this matters:** Supabase recommends a separate secret key per backend component to reduce blast radius if a key is compromised. The product currently has one backend component that touches Supabase (`src/load_supabase.py`). This affects how many env variables are defined in `.env.example`.
- [x] Option A: Single `sb_secret_...` key for all backend operations — one env variable (`SUPABASE_SECRET_KEY`). *(recommended)*
- [ ] Option B: One `sb_secret_...` key per backend component — e.g., `SUPABASE_ETL_SECRET_KEY` for `load_supabase.py`; additional keys for future components.
- [ ] Other: ___

**Q010**: Should the React app validate the key format prefix (reject `sb_secret_...` keys in the frontend)?
> **Why this matters:** Currently `supabase.js` only checks that the key is non-empty. Adding a prefix check (`sb_publishable_` check, reject `sb_secret_` or JWT format) would prevent accidentally bundling a secret key into the browser without warning.
- [x] Option A: Add prefix validation — reject keys starting with `sb_secret_` or matching a JWT pattern (3 dot-separated segments); set `credentialsError` with a descriptive message. *(recommended)*
- [ ] Option B: Keep existing non-empty check only — no format validation; permissive for any key format.
- [ ] Other: ___
