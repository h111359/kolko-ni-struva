---
state:
  request_id: R-20260711-0835
  title: Migrate from Supabase to Azure SQL Database
  status: questions_generated
  input_verification_result: valid
  context_verification_result: valid
options:
  minimum_questions: 5
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input



## Questions

**Q001**: Which REST endpoint structure should the Azure Functions API use?
> **Why this matters:** The endpoint design affects React client refactoring scope, API discoverability, and alignment with REST principles.
- [ ] Option A: Resource-based REST (e.g., GET /api/landing-page, GET /api/options/settlements)
- [ ] Option B: Operation-based REST (e.g., GET /api/queries/landing-page-rows, GET /api/filters/settlements)
- [x] Option C: Hybrid design (e.g., GET /api/landing-page/rows, GET /api/options/settlements) *(recommended)*
- [ ] Other: ___

**Q002**: How should the retention window be configured?
> **Why this matters:** The configuration mechanism affects operator workflows, deployment complexity, and consistency with existing ETL configuration patterns.
- [x] Option A: config.ini file (azure_sql_retention_days key) *(recommended)*
- [ ] Option B: Environment variable (AZURE_SQL_RETENTION_DAYS in .env)
- [ ] Option C: Command-line argument (--retention-days flag on load_azure_sql.py)
- [ ] Option D: Database parameter table (config table in Azure SQL)
- [ ] Other: ___

**Q003**: What level of error handling should the React application implement for REST API errors?
> **Why this matters:** Error handling sophistication affects user experience, supportability, implementation complexity, and troubleshooting capabilities.
- [ ] Option A: Minimal (generic "Unable to load data" message)
- [ ] Option B: Status-code-based (different messages for 400, 404, 500, 503 with retry logic)
- [x] Option C: Structured error responses (error codes, user-friendly messages, correlation IDs) *(recommended)*
- [ ] Other: ___

**Q004**: What testing strategy should be used to verify functional equivalence between Supabase and Azure?
> **Why this matters:** Testing approach determines migration confidence, implementation time, and regression safety throughout the migration.
- [ ] Option A: Manual testing (operator manually verifies behavior)
- [ ] Option B: Automated UI tests (Playwright/Cypress comparing both versions)
- [ ] Option C: Data comparison tests (pytest comparing stored procedure results against RPC calls)
- [x] Option D: Hybrid approach (data comparison tests + critical workflow smoke tests) *(recommended)*
- [ ] Other: ___

**Q005**: What migration cutover strategy should be used?
> **Why this matters:** Cutover strategy determines deployment risk, rollback complexity, migration duration, and operational overhead during transition.
- [x] Option A: Big-bang cutover (deploy Azure and decommission Supabase simultaneously)
- [ ] Option B: Blue-green deployment (deploy Azure, test, switch when ready, keep Supabase as backup) *(recommended)*
- [ ] Option C: Phased migration with feature flags (gradually route users from Supabase to Azure)
- [ ] Other: ___
