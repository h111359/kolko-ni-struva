## Goal

Remove all UI occurrences of the "Ефективна цена" label from the React analytics app across the three components where it appears: the column table in `FileRowsPanel.jsx` (Files page), the row-detail modal in `FileRowDetailModal.jsx` (Files page), and the record-detail modal in `RecordDetailModal.jsx` (Report 2 page). Update the two test assertions that check for the presence of the removed UI elements. The underlying `calculatedPrice` data field and `calculatePrice()` function in `dataService.js` are retained unchanged because they continue to serve Report 2 and Report 3.

## Constraints

- The `calculatedPrice` data field MUST NOT be removed from row objects or from `dataService.js`; it is still used by Report 2 ("Цена" column) and Report 3 ("Цена" column).
- No changes to `dataService.js` source logic are permitted.
- No changes to Report 1, Report 2, or Report 3 column definitions or table headers are permitted.
- No changes to `dist/` build artifacts — they are regenerated by `npm run build` on next deploy.
- Only the three JSX components and two test files identified in analysis are modified.

## Success criteria

- The string "Ефективна цена" does not appear in any rendered HTML of any page of the React app.
- All Vitest tests in `react-app/` pass with exit code 0 after the changes.
- Report 2 "Цена" column and Report 3 "Цена" column still render numeric values correctly (no regressions).
- The `FileRowsPanel` table renders exactly 11 column headers (the "Ефективна цена" header is absent).

## Plan

### Task 1: Remove Ефективна цена column from FileRowsPanel.jsx

#### Intent
Remove the `calculatedPrice` / "Ефективна цена" entry from the `buildColumns()` function so the column no longer appears in the Files page table.

#### Outputs
Modified `react-app/src/components/FileRowsPanel.jsx` — `buildColumns()` returns 11 column definitions instead of 12.

#### Procedure

In `react-app/src/components/FileRowsPanel.jsx`, locate the `buildColumns()` function and delete the line:

```
    { key: 'calculatedPrice',   label: 'Ефективна цена',                 type: 'numeric' },
```

No other changes to `FileRowsPanel.jsx` are required: the column rendering loop iterates over the column definitions array, so removing the entry from `buildColumns()` automatically removes the column header, all corresponding data cells, and the filter input for that column.

#### Done criteria

- `buildColumns()` in `react-app/src/components/FileRowsPanel.jsx` returns exactly 11 entries.
- The string "Ефективна цена" does not appear anywhere in `react-app/src/components/FileRowsPanel.jsx`.
- No syntax errors in the file.

#### Dependencies

None.

#### Risk notes

None.

---

### Task 2: Remove Ефективна цена field from FileRowDetailModal.jsx

#### Intent
Remove the "Ефективна цена" label and its associated price value from the `FileRowDetailModal` detail view.

#### Outputs
Modified `react-app/src/components/FileRowDetailModal.jsx` — the `<dt>Ефективна цена:</dt>` and its sibling `<dd>` element are deleted.

#### Procedure

In `react-app/src/components/FileRowDetailModal.jsx`, locate and remove the following two consecutive lines (approximately at lines 97–98):

```
          <dt>Ефективна цена:</dt>
          <dd>{formatPrice(row.calculatedPrice)}</dd>
```

#### Done criteria

- The string "Ефективна цена" does not appear anywhere in `react-app/src/components/FileRowDetailModal.jsx`.
- The `<dl>` structure in the modal remains syntactically valid (no unclosed or orphaned tags).

#### Dependencies

None.

#### Risk notes

None.

---

### Task 3: Remove Ефективна цена field from RecordDetailModal.jsx

#### Intent
Remove the "Ефективна цена" label and its associated price value from the `RecordDetailModal` detail view.

#### Outputs
Modified `react-app/src/components/RecordDetailModal.jsx` — the `<dt style={{ fontWeight: 600, color: '#555' }}>Ефективна цена:</dt>` and its sibling `<dd>` element are deleted.

#### Procedure

In `react-app/src/components/RecordDetailModal.jsx`, locate and remove the following two consecutive lines (approximately at lines 103–104):

```
          <dt style={{ fontWeight: 600, color: '#555' }}>Ефективна цена:</dt>
          <dd style={{ margin: 0 }}>{row.calculatedPrice != null ? row.calculatedPrice.toFixed(2) : '—'}</dd>
```

#### Done criteria

- The string "Ефективна цена" does not appear anywhere in `react-app/src/components/RecordDetailModal.jsx`.
- The `<dl>` structure in the modal remains syntactically valid (no unclosed or orphaned tags).

#### Dependencies

None.

#### Risk notes

None.

---

### Task 4: Remove failing test assertions

#### Intent
Delete the two test assertions that check for the presence of "Ефективна цена" in the DOM, which will fail after Tasks 1–3 remove the rendered elements.

#### Outputs
Modified `react-app/src/components/FileRowsPanel.test.jsx` — assertion for "Ефективна цена" removed.
Modified `react-app/src/components/FileRowDetailModal.test.jsx` — assertion for "Ефективна цена:" removed.

#### Procedure

In `react-app/src/components/FileRowsPanel.test.jsx`, locate and remove the following single line (approximately line 131):

```
    expect(screen.getByText('Ефективна цена')).toBeInTheDocument();
```

In `react-app/src/components/FileRowDetailModal.test.jsx`, locate and remove the following single line (approximately line 90):

```
    expect(screen.getByText('Ефективна цена:')).toBeInTheDocument();
```

Do NOT add a negative assertion; the absence of the column is implicitly confirmed by the remaining column-header enumeration assertions that are still present in each test.

#### Done criteria

- The string "Ефективна цена" does not appear anywhere in `react-app/src/components/FileRowsPanel.test.jsx` or `react-app/src/components/FileRowDetailModal.test.jsx`.
- No test case is left with a dangling or broken `expect()` call.

#### Dependencies

Tasks 1 and 2 (the removed assertions refer to elements deleted in those tasks; updating them beforehand causes them to fail immediately).

#### Risk notes

None.

---

### Task 5: Run test suite and verify

#### Intent
Confirm that the full Vitest test suite passes with exit code 0 after Tasks 1–4.

#### Outputs
Terminal output from `npm test -- --run` in `react-app/` showing all tests pass.

#### Procedure

From the `react-app/` directory, run:

```
npm test -- --run
```

Review the output. Expected: all test suites pass; zero failures. If any test fails with a message referencing "Ефективна цена", revisit Task 4. If any test fails with an unrelated message, assess whether it was pre-existing or introduced by this request.

#### Done criteria

- Exit code 0 from `npm test -- --run`.
- Zero test failures.

#### Dependencies

Tasks 1–4.

#### Risk notes

- `Report2.test.jsx` and `Report3.test.jsx` were inspected during analysis and contain no column-count assertions tied to `calculatedPrice`. The risk of regressions in those files is low.

---

### Task 6: Update context.md

#### Intent
Update `.aib_memory/context.md` to reflect that the "Ефективна цена" column has been removed and the Files page table now has 11 columns.

#### Outputs
Updated `.aib_memory/context.md` — two "12-column" / "12 display fields" references corrected to "11"; update annotation added.

#### Procedure

In `.aib_memory/context.md`, locate the passage in Functional Capability 7 that contains the phrase "12-column table of individual price-fact records" and replace "12-column" with "11-column".

In the same section, locate the phrase "showing all 12 display fields and surrogate keys" and replace "12 display fields" with "11 display fields".

Add the following update annotation to the chronological update list at the top of the file, after the most recent `> Updated by ...` line:

```
> Updated by R-20260518-2134: "Ефективна цена" column removed from FileRowsPanel (Files page table now 11 columns), FileRowDetailModal, and RecordDetailModal; calculatedPrice data field retained for Report 2 and Report 3 "Цена" columns.
```

**Acceptance test:**
- (1) The string "12-column" does not appear in `.aib_memory/context.md` after the update.
- (2) The string "12 display fields" does not appear in `.aib_memory/context.md` after the update.
- (3) The update annotation for R-20260518-2134 is present in the update history section.

#### Dependencies

Tasks 1–3.

#### Risk notes

None.
