## Active request
R-20260418-2209 — Keep raw download and nomenclatures

## Options
- [ ] No changes — provide answer only
- [ ] Skip analysis document generation

## Input

Create refresh.bat script in the main folder which when executed  runs the whole ETL available

Create menu.bat script in the main folder which run in a terminal and shows statistics what days are available and provides a menu for manually execution of possible actions

Move exract.py in src/ folder

Create a folder data/schema and in it create a star schema with extracted data from raw. Aim is minimal space consumption still data in human readable format. 