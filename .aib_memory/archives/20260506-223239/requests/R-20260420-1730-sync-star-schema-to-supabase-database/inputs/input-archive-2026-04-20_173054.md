## Active request
R-20260419-0854 — ETL scripts, menu, and star-schema data layer

## Options
- [ ] No changes — provide answer only
- [ ] Skip analysis document generation

## Input

Create a new module for creation if not exists or update with the latest stage and insertion the last available day in a schema in supabase. `.env` contains connection details. A separate option in the menu should allow simple for the user way to update the db.
