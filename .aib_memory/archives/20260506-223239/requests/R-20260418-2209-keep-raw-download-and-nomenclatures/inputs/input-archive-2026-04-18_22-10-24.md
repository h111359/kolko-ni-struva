## Active request
R-20260418-0120 — Check pipeline.log and find problems

## Options
- [ ] No changes — provide answer only
- [ ] Skip analysis document generation

## Input

Start over.
Remove all the functionality except download of the raw data in `data/raw`
Keep also the folder `data/nomenclatures` and its content
`data/quality` to be removed
`data/schema` to be removed
`docs` to be removed
Review the scripts in `src` and remove everything not related to the download of the new files in `data/raw`
Keep `.env`
Check what `extract.py` is doing and if not part of the solution - remove it.
Update `.aib_memory/context.md`
Update `README.md`
