## Status
R-20260525-1400 — Total redesign of the frontend
State: questions_generated

## Options
- Minimum questions: 0

## Input

## Questions

**Q001**: How should the landing page load `fact_prices_lookback` rows for the selected date?
> **Why this matters:** A single date contains approximately 1.3 million rows. The loading strategy determines whether filter interactions are consistent across the full dataset or only across a single fetched page, and significantly affects implementation scope and Supabase provisioning requirements.
- [ ] Option A: Server-side paginated fetch using Supabase `.range()` with filter predicates pushed to Supabase (`.eq()`, `.ilike()`, `.gte()`, `.lte()`); product-name filter may require an additional client-side step or separate RPC; ~1.5 days effort *(recommended)*
- [x] Option B: Single generic Supabase PostgreSQL RPC accepting all filter and pagination parameters; all filtering, enrichment, and pagination in one SQL call; requires authoring and provisioning a new RPC in `load_supabase.py`; ~3 days effort
- [ ] Other: ___

**Q002**: Should the five dropdown filters (Населено място, Категория, Верига, Магазин, Дата) operate independently, or should selecting "Верига" (chain) narrow the "Магазин" (store) dropdown to only that chain's stores?
> **Why this matters:** The "Магазин" dropdown contains approximately 4,824 entries; without dependent filtering it is impractical to browse. Whether to implement cross-filtering (and how much) affects implementation scope and UX quality.
- [ ] Option A: All five dropdowns are fully independent — show the complete option list regardless of other selections (strict literal interpretation of the input; simplest implementation) *(recommended)*
- [ ] Option B: One-directional cascade — selecting "Верига" filters "Магазин" to only that chain's stores; all other dropdowns remain independent; uses existing `dims.stores` with `company_key`; no new RPC required
- [x] Option C: Full multi-dimensional cross-filtering across all five dropdowns — each dropdown shows only options with data for the current combination; mirrors the Report2 pattern; requires multiple new or extended RPCs
- [ ] Other: ___
