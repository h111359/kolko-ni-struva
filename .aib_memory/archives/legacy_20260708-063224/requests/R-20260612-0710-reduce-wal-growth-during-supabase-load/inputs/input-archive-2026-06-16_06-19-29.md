## Status
R-20260612-0710 — Reduce WAL growth during Supabase load
State: questions_generated

## Options
- Minimum questions: 0

## Input

## Questions

**Q001**: Which write strategy should the implementation use for `fact_prices_lookback`: `COPY-based full replacement`, `Incremental retained-window merge`, or `Keep batched INSERT full replacement`? If you want a different strategy, name it explicitly.
> **Why this matters:** This choice determines whether the implementation optimizes the current full-refresh design or redesigns the fact sync contract to reduce WAL more aggressively.
- Answer: COPY-based full replacement

**Q002**: How should `landing_page_row_projection` be handled: `Keep physical projection full rebuild`, `Targeted projection refresh`, or `Replace physical projection with view or materialized view workflow`? If you want a different approach, describe it.
> **Why this matters:** The projection refresh is a second major recurring write surface, so this decision determines how much of the WAL reduction effort can come from changing the derived-table strategy.
- Answer: Replace physical projection with view

**Q003**: Must `src/load_supabase.py` remain a single self-provisioning sync command, or is it acceptable to separate one-time schema or index provisioning from the recurring low-WAL data-sync path?
> **Why this matters:** This determines whether the implementation can reduce recurring write pressure by splitting steady-state data loads from schema-provisioning behavior.
- Answer: remain a single self-provisioning sync command

