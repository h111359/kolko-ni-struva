## Status
No active request
State: idle

## Options
- Minimum questions: 0

## Input

Review `.gitignore` and add there all files and folders, which are not part of the application code.
The application still should be able to be restored fully from the remote repo (and it will be)
Files with secrets and credentials should be excluded. If they are needed to be stored in the repo in order the app to be installed and no possibility to be configured on the destination - alarm and ask the user.

Act as experienced DevOps engineer and sort out the .gitignore, repo content and way of installation on another machine only from the shared github repo