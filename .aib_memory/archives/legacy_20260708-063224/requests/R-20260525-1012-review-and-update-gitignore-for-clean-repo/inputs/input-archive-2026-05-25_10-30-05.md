## Status
R-20260525-1012 — Review and update .gitignore for clean repo
State: questions_generated

## Options
- Minimum questions: 0

## Input

## Questions

**Q001**: How should the credentials already committed in git history be handled?

Two files containing live credentials (`netlify token.txt` with the Netlify API token, and `curl https ekootljybgoenduwprbw.txt` with the Supabase service_role key and PostgreSQL password) are already in git history. Adding them to `.gitignore` stops future commits but does NOT remove them from existing history.
> **Why this matters:** If git history is not rewritten, any person who clones the repository and inspects prior commits can recover all three credentials. If the repository ever becomes public, these credentials would be immediately accessible to anyone.
- [x] Option A: History rewrite + credential rotation — use `git filter-repo` to remove both files from all historical commits, force-push to remote, and simultaneously revoke/rotate all three exposed credentials (Netlify token, Supabase service_role key, PostgreSQL password). *(recommended)*
- [ ] Option B: Credential rotation only — leave git history unchanged; immediately revoke the Netlify token, rotate the Supabase service_role key, and change the PostgreSQL password so the historical credentials are rendered unusable. Add both files to `.gitignore` and remove from working tree.
- [ ] Other: ___

---

**Q002**: Should the `lab/` folder be excluded from git?

The `lab/` folder is currently tracked in git. It contains hundreds of raw CSV price-data files and a ZIP archive from 2026-05-13 (a single government-portal download date). Its purpose is ambiguous — it may be intentional reference/sample data for development, or it may be an oversight similar to `data/raw/` (which is already excluded).
> **Why this matters:** If `lab/` is excluded, the repository becomes significantly leaner and consistent with the exclusion of `data/`. If it is kept, future developers have a ready sample dataset for local ETL development without needing to run a download.
- [x] Option A: Exclude `lab/` — add `lab/` to `.gitignore` and untrack all existing files. The files remain on the local machine. *(recommended)*
- [ ] Option B: Keep `lab/` tracked — retain the current behavior; the sample dataset is shared via the repository.
- [ ] Other: ___

---

**Q003**: How should `config.ini` be handled in the repository?

`config.ini` is currently tracked. It contains application configuration settings (URL, retries, log level) and machine-written operational state (`last_downloaded_date = 2026-05-24`, `last_processed_date = 2026-05-24`). There are no secrets in this file.
> **Why this matters:** The state dates in `config.ini` affect ETL behavior on a fresh clone: the ETL will treat dates before 2026-05-24 as already processed and skip them. For a developer setting up on a new machine, this may or may not be the desired starting point.
- [ ] Option A: Keep tracked as-is — the committed state dates act as a valid baseline; the ETL starts from the current operational date on a fresh clone.
- [x] Option B: Exclude `config.ini`, track `config.ini.example` — follow the same pattern as `.env` / `.env.example`; the example has placeholder/empty state values; developer copies on first setup. *(recommended)*
- [ ] Option C: Keep tracked but reset state section — track `config.ini` but set `last_downloaded_date =` and `last_processed_date =` to empty so a fresh clone starts with no prior state.
- [ ] Other: ___

---

**Q004**: How should the EKATTE nomenclature files be made available for fresh installation?

`src/transform.py` requires six JSON files under `data/nomenclatures/` at runtime (EKATTE government settlement registry). These files are currently excluded from git by the `data/` rule in `.gitignore`. Without them, the ETL transform step cannot run on a fresh clone. The files are small, static, and contain no credentials — they are public Bulgarian government reference data.
> **Why this matters:** The developer stated the application should be fully restorable from the repository. The nomenclature files are a hard runtime dependency of `src/transform.py`; their absence causes the ETL to fail on first run on any new machine.
- [x] Option A: Track nomenclature files — add `!data/nomenclatures/` exception to `.gitignore` so these files are committed to the repo. Simple and reliable. *(recommended)*
- [ ] Option B: Add a setup script — keep nomenclatures excluded; add a `setup.sh`/`setup.py` that downloads them from an authoritative public URL. Keeps data out of the repo but requires internet access and URL stability.
- [ ] Option C: Document in README only — keep nomenclatures excluded; add a manual step in `README.md` for developers to obtain the files themselves.
- [ ] Other: ___

