## Active request
No active request

## Options
- [ ] No changes — provide answer only
- [ ] Skip analysis document generation
- Question threshold: [ ] 1 (all)  [ ] 2  [x] 3  [ ] 4  [ ] 5 (mandatory only)

## Input

The table with detail records of a file in "Филове" shall fit to the area, no horizontal scrolling. Reduce the font if needed, wrap text. Do it it the most user friendly way.

Also instead of "Д-1" and "Д-2" - write the exact day in format DD.MM.YYYY

There is an issue with pagination when filter is applied. The filered items remain on same pages instead of condense in just the first pages. The left items after filtering of the items in the pages should be dense positioned in the first pages and the number of pages - reduced as much as needed. Change of the filter should generated new set of pages.