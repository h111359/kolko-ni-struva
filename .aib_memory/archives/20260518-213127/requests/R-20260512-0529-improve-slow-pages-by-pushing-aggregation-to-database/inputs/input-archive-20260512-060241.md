## Active request
No active request

## Options
- [ ] No changes — provide answer only
- [ ] Skip analysis document generation
- Question threshold: [ ] 1 (all)  [ ] 2  [x] 3  [ ] 4  [ ] 5 (mandatory only)

## Input

Some pages are very slow. The performance should be improved.

Check all cases for iteration with the database. Refactor the code so as much as possible to push the queries back to the database. Avoid data processing in the frontend as much as possible. 

For example in "Цени по категория" the grouping should happen in the database, not in the frontend.