/**
 * test-setup.js: Vitest global setup for the Kolko Ni Struva React app tests.
 * Imports @testing-library/jest-dom to extend Vitest's expect with DOM matchers
 * such as toBeInTheDocument(), toHaveTextContent(), and toBeVisible().
 */
import '@testing-library/jest-dom';
